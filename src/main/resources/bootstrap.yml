spring:
  application:
    name: zuul-server
  cloud:
    config:
      uri: http://localhost:8880
      label: master
      profile: dev
#      discovery:
#        enabled: true
#        service-id: config-server

